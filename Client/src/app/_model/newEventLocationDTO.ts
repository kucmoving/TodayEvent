export interface newEventLocationDTO{

  name:string;
  latitude: number;
  longitude: number;
}

export interface eventLocationDTO{
  name:string;
  latitude: number;
  longitude: number;
}
