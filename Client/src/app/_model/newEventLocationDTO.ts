export interface newEventLocationDTO{

  name:string;
  latitude: number;
  longitude: number;
}

export interface eventLocationDTO{
  id:number,
  name:string;
  latitude: number;
  longitude: number;
}
